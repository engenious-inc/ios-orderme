appId: ${APP_ID}
name: "Playback with WireMock"
env:
  WIREMOCK_BASE: ${WIREMOCK_BASE}

---
- launchApp:
    clearState: true
    arguments:
      BASE_URL: ${WIREMOCK_BASE}
      IS_E2E: true

# Ваши UI-шага, которые используют уже сохранённые стабы
# - tapOn: "Refresh"
# - assertVisible: "Some mocked data"

